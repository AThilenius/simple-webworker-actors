{
  "name": "@athilenius/simple-webworker-actors",
  "version": "0.2.1",
  "license": "Dual-licensed MIT + APACHE V2.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "module": "dist/index.mjs",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "start": "tsup --watch --dts src/index.ts",
    "build": "tsup --clean --sourcemap external --format cjs --format esm --dts src/index.ts src/index.ts"
  },
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "tsup": "^5.12.7",
    "typescript": "^4.6.4"
  }
}
